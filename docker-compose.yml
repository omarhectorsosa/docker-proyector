version: "3"

services:
  web: 
    image: proyector:v6
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      APP_ENV: dev
    volumes:
      - ./build/:/var/www/
    ports:
      - 3000:80  
    networks:
      - proyector 
networks:
  proyector:       
